name: cryload

description: "HTTP load testing for CI/CD — multi-core, coordinated-omission corrected, single binary, JSON output"

version: 6.0.0

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
    branch: master

authors:
  - Serdar Dogruyol <dogruyolserdar@gmail.com>

targets:
  cryload:
    main: src/main.cr

crystal: 1.21.0

license: MIT
